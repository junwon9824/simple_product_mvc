<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- Type Aliases 설정 -->
	<typeAliases>
		<!-- 여기에 타입 별칭 설정 -->
	</typeAliases>

	<!-- 매퍼 파일 설정 -->
	<mappers>
		<mapper resource="mappers/DestinationMapper.xml" />

		<!-- 다른 매퍼 파일도 추가할 수 있음 -->
	</mappers>

	<!-- 추가적인 설정 옵션들 -->
</configuration>
